category: airplane
data_root: data/ModelNet40
split: train
num_points: 2048
timesteps: 1000
beta_schedule: cosine
hidden_dim: 256
batch_size: 32
epochs: 500
lr: 0.0001
weight_decay: 0.01
num_workers: 0
save_dir: checkpoints
log_every: 20
save_every: 50
device: cuda
